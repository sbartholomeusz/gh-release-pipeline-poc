name: create-release

on:
  # Run workflow manually
  workflow_dispatch:

defaults:
  run:
    shell: bash
    working-directory: src

jobs:   
  release:
    permissions:
      # Required to publish release artifacts
      contents: write
      
    uses: sbartholomeusz/gh-actions-common/.github/workflows/dotnet-release.yml@feature/rework-sem-versioning
    #secrets: inherit